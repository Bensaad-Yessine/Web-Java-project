{% extends 'base.html.twig' %}

{% block title %}MatiereClasse{% endblock %}

{% block body %}
    <h1>MatiereClasse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matiere_classe.id }}</td>
            </tr>
            <tr>
                <th>Coefficient</th>
                <td>{{ matiere_classe.coefficient }}</td>
            </tr>
            <tr>
                <th>Chargehoraire</th>
                <td>{{ matiere_classe.chargehoraire }}</td>
            </tr>
            <tr>
                <th>Scorecomplexite</th>
                <td>{{ matiere_classe.scorecomplexite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_matiere_classe_index') }}">back to list</a>

    <a href="{{ path('app_matiere_classe_edit', {'id': matiere_classe.id}) }}">edit</a>

    {{ include('matiere_classe/_delete_form.html.twig') }}
{% endblock %}
