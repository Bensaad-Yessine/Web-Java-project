{% extends 'base.html.twig' %}

{% block title %}Ajouter une recommandation{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>Ajouter une recommandation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-success mt-3">Enregistrer</button>
    {{ form_end(form) }}

    <a class="btn btn-secondary mt-3" href="{{ path('app_recommendation_bien_etre_index') }}">
        Retour
    </a>
</div>
{% endblock %}
