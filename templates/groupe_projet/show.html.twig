{% extends 'base.html.twig' %}

{% block title %}GroupeProjet{% endblock %}

{% block body %}
    <h1>GroupeProjet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ groupe_projet.id }}</td>
            </tr>
            <tr>
                <th>GroupeId</th>
                <td>{{ groupe_projet.groupeId }}</td>
            </tr>
            <tr>
                <th>NomProjet</th>
                <td>{{ groupe_projet.nomProjet }}</td>
            </tr>
            <tr>
                <th>Matiere</th>
                <td>{{ groupe_projet.matiere }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_groupe_projet_index') }}">back to list</a>

    <a href="{{ path('app_groupe_projet_edit', {'id': groupe_projet.id}) }}">edit</a>

    {{ include('groupe_projet/_delete_form.html.twig') }}
{% endblock %}
