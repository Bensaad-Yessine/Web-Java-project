<form method="post" action="{{ path('app_preference_alerte_delete', {'id': preference_alerte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ preference_alerte.id) }}">
    <button class="btn">Delete</button>
</form>
