<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Corona Admin - Modifier Utilisateur</title>
    <!-- plugins:css -->
    <link rel="stylesheet" href="{{ asset('assets/vendors/mdi/css/materialdesignicons.min.css') }}">
    <link rel="stylesheet" href="{{ asset('assets/vendors/ti-icons/css/themify-icons.css') }}">
    <link rel="stylesheet" href="{{ asset('assets/vendors/css/vendor.bundle.base.css') }}">
    <link rel="stylesheet" href="{{ asset('assets/vendors/font-awesome/css/font-awesome.min.css') }}">
    <!-- Layout styles -->
    <link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">
    <link rel="shortcut icon" href="{{ asset('assets/images/favicon.png') }}" />
    <style>
      .form-container {
        max-width: 900px;
        margin: 0 auto;
        animation: fadeIn 0.5s ease-in;
      }
      @keyframes fadeIn {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
      }
      .form-section {
        background: #fff;
        border-radius: 10px;
        padding: 25px;
        margin-bottom: 25px;
        box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        transition: transform 0.3s ease, box-shadow 0.3s ease;
      }
      .form-section:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 12px rgba(0,0,0,0.15);
      }
      .form-section-title {
        color: #2c3e50;
        border-bottom: 2px solid #FF9800;
        padding-bottom: 10px;
        margin-bottom: 20px;
        font-weight: 600;
      }
      .user-info-card {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        border-radius: 10px;
        padding: 25px;
        margin-bottom: 30px;
        animation: slideInLeft 0.6s ease;
      }
      @keyframes slideInLeft {
        from { opacity: 0; transform: translateX(-30px); }
        to { opacity: 1; transform: translateX(0); }
      }
      .avatar-circle {
        width: 80px;
        height: 80px;
        background: white;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 15px;
        color: #667eea;
        font-size: 32px;
        font-weight: bold;
      }
      .form-group-animated {
        position: relative;
        margin-bottom: 25px;
      }
      .form-group-animated label {
        display: block;
        margin-bottom: 8px;
        font-weight: 500;
        color: #495057;
        transition: color 0.3s ease;
      }
      .btn-update {
        background: linear-gradient(45deg, #FF9800, #F57C00);
        border: none;
        padding: 12px 30px;
        font-size: 16px;
        font-weight: 500;
        transition: all 0.3s ease;
      }
      .btn-update:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(255, 152, 0, 0.4);
      }
      .current-value {
        background: #f8f9fa;
        border-left: 4px solid #4CAF50;
        padding: 10px 15px;
        margin-bottom: 15px;
        border-radius: 5px;
      }
      .password-note {
        background: #FFF3CD;
        border: 1px solid #FFEEBA;
        border-radius: 5px;
        padding: 10px;
        font-size: 0.9rem;
        color: #856404;
        margin-top: 10px;
      }
    </style>
  </head>
  <body>
    <div class="container-scroller">
      <!-- partial:partials/_sidebar.html -->
      <nav class="sidebar sidebar-offcanvas" id="sidebar">
        <div class="sidebar-brand-wrapper d-none d-lg-flex align-items-center justify-content-center fixed-top">
          <a class="sidebar-brand brand-logo" href="index.html"><img src="assets/images/logo.svg" alt="logo" /></a>
          <a class="sidebar-brand brand-logo-mini" href="index.html"><img src="assets/images/logo-mini.svg" alt="logo" /></a>
        </div>
        <ul class="nav">
          <li class="nav-item profile">
            <div class="profile-desc">
              <div class="profile-pic">
                <div class="count-indicator">
                  <img class="img-xs rounded-circle " src="assets/images/faces/face15.jpg" alt="">
                  <span class="count bg-success"></span>
                </div>
                <div class="profile-name">
                  <h5 class="mb-0 font-weight-normal">Henry Klein</h5>
                  <span>Gold Member</span>
                </div>
              </div>
            </div>
          </li>
          <li class="nav-item nav-category">
            <span class="nav-link">Navigation</span>
          </li>
          <li class="nav-item menu-items">
            <a class="nav-link" href="{{ path('app_user_index') }}">
              <span class="menu-icon">
                <i class="mdi mdi-speedometer"></i>
              </span>
              <span class="menu-title">Dashboard</span>
            </a>
          </li>
          <li class="nav-item menu-items active">
            <a class="nav-link" href="{{ path('app_user_index') }}">
              <span class="menu-icon">
                <i class="mdi mdi-account-group"></i>
              </span>
              <span class="menu-title">Utilisateurs</span>
            </a>
          </li>
        </ul>
      </nav>
      <!-- partial -->
      <div class="container-fluid page-body-wrapper">
        <!-- partial:partials/_navbar.html -->
        <nav class="navbar p-0 fixed-top d-flex flex-row">
          <div class="navbar-brand-wrapper d-flex d-lg-none align-items-center justify-content-center">
            <img src="{{ asset('assets/images/logo.svg') }}" alt="logo" />
          </div>
          <div class="navbar-menu-wrapper flex-grow d-flex align-items-stretch">
            <button class="navbar-toggler navbar-toggler align-self-center" type="button" data-toggle="minimize">
              <span class="mdi mdi-menu"></span>
            </button>
            <ul class="navbar-nav navbar-nav-right">
              <li class="nav-item dropdown">
                <a class="nav-link" id="profileDropdown" href="#" data-bs-toggle="dropdown">
                  <div class="navbar-profile">
                    <img class="img-xs rounded-circle" src="{{ asset('assets/images/faces/face15.jpg') }}" alt="">
                    <p class="mb-0 d-none d-sm-block navbar-profile-name">Henry Klein</p>
                    <i class="mdi mdi-menu-down d-none d-sm-block"></i>
                  </div>
                </a>
                <div class="dropdown-menu dropdown-menu-end navbar-dropdown preview-list" aria-labelledby="profileDropdown">
                  <h6 class="p-3 mb-0">Profile</h6>
                  <div class="dropdown-divider"></div>
                  <a class="dropdown-item preview-item">
                    <div class="preview-thumbnail">
                      <div class="preview-icon bg-dark rounded-circle">
                        <i class="mdi mdi-logout text-danger"></i>
                      </div>
                    </div>
                    <div class="preview-item-content">
                      <p class="preview-subject mb-1">Log out</p>
                    </div>
                  </a>
                </div>
              </li>
            </ul>
            <button class="navbar-toggler navbar-toggler-right d-lg-none align-self-center" type="button" data-toggle="offcanvas">
              <span class="mdi mdi-format-line-spacing"></span>
            </button>
          </div>
        </nav>
        <!-- partial -->
        <div class="main-panel">
          <div class="content-wrapper">
            <div class="row">
              <div class="col-12 grid-margin">
                <div class="card">
                  <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center mb-4">
                      <h4 class="card-title">✏️ Modifier l'utilisateur #{{ user.id }}</h4>
                      <a href="{{ path('app_user_index') }}" class="btn btn-secondary btn-icon-text">
                        <i class="mdi mdi-arrow-left"></i> Retour à la liste
                      </a>
                    </div>

                    <!-- User Info Card -->
                    <div class="user-info-card">
                      <div class="row align-items-center">
                        <div class="col-md-3 text-center">
                          <div class="avatar-circle">
                            {{ user.prenom|first|upper }}{{ user.nom|first|upper }}
                          </div>
                        </div>
                        <div class="col-md-9">
                          <h3 class="mb-2">{{ user.prenom }} {{ user.nom }}</h3>
                          <p class="mb-1"><i class="mdi mdi-email"></i> {{ user.email }}</p>
                          <p class="mb-1"><i class="mdi mdi-account"></i> 
                            <span class="badge badge-light">
                              {% if user.role == 'Étudiant' %}
                                <span class="text-primary">Étudiant</span>
                              {% elseif user.role == 'Administrateur' %}
                                <span class="text-danger">Administrateur</span>
                              {% else %}
                                {{ user.role }}
                              {% endif %}
                            </span>
                          </p>
                          <p class="mb-0"><i class="mdi mdi-calendar"></i> Créé le: {{ user.dateDeNaissance ? user.dateDeNaissance|date('d/m/Y') : 'Non spécifiée' }}</p>
                        </div>
                      </div>
                    </div>

                    <div class="form-container">
                      {{ form_start(form, {'attr': {'class': 'form-sample', 'id': 'editForm'}}) }}
                        
                        <!-- Personal Information Section -->
                        <div class="form-section">
                          <h5 class="form-section-title">
                            <i class="mdi mdi-account-edit"></i> Informations personnelles
                          </h5>
                          
                          <div class="current-value">
                            <small class="text-muted">Valeurs actuelles:</small>
                            <div><strong>Nom:</strong> {{ user.nom }} | <strong>Prénom:</strong> {{ user.prenom }}</div>
                          </div>

                          <div class="row">
                            <div class="col-md-6">
                              <div class="form-group-animated">
                                <label>Nom</label>
                                <div class="input-group">
                                  <div class="input-group-prepend">
                                    <span class="input-group-text"><i class="mdi mdi-account"></i></span>
                                  </div>
                                  {{ form_widget(form.nom, {'attr': {
                                    'class': 'form-control',
                                    'placeholder': 'Entrez le nom'
                                  }}) }}
                                </div>
                                {{ form_errors(form.nom) }}
                              </div>
                            </div>
                            <div class="col-md-6">
                              <div class="form-group-animated">
                                <label>Prénom</label>
                                <div class="input-group">
                                  <div class="input-group-prepend">
                                    <span class="input-group-text"><i class="mdi mdi-account-outline"></i></span>
                                  </div>
                                  {{ form_widget(form.prenom, {'attr': {
                                    'class': 'form-control',
                                    'placeholder': 'Entrez le prénom'
                                  }}) }}
                                </div>
                                {{ form_errors(form.prenom) }}
                              </div>
                            </div>
                          </div>

                          <div class="row">
                            <div class="col-md-6">
                              <div class="form-group-animated">
                                <label>Email</label>
                                <div class="input-group">
                                  <div class="input-group-prepend">
                                    <span class="input-group-text"><i class="mdi mdi-email"></i></span>
                                  </div>
                                  {{ form_widget(form.email, {'attr': {
                                    'class': 'form-control',
                                    'value': user.email
                                  }}) }}
                                </div>
                                {{ form_errors(form.email) }}
                              </div>
                            </div>
                            <div class="col-md-6">
                              <div class="form-group-animated">
                                <label>Date de naissance</label>
                                <div class="input-group">
                                  <div class="input-group-prepend">
                                    <span class="input-group-text"><i class="mdi mdi-calendar"></i></span>
                                  </div>
                                  {{ form_widget(form.DateDeNaissance, {'attr': {
                                    'class': 'form-control',
                                    'value': user.dateDeNaissance ? user.dateDeNaissance|date('Y-m-d') : ''
                                  }}) }}
                                </div>
                                {{ form_errors(form.DateDeNaissance) }}
                              </div>
                            </div>
                          </div>
                        </div>

                        <!-- Academic Information Section -->
                        <div class="form-section">
                          <h5 class="form-section-title">
                            <i class="mdi mdi-school"></i> Informations académiques
                          </h5>
                          
                          <div class="current-value">
                            <small class="text-muted">Valeurs actuelles:</small>
                            <div><strong>Classe:</strong> {{ user.classe ?: 'Non spécifiée' }} | <strong>Spécialité:</strong> {{ user.specialiteFuture ?: 'Non spécifiée' }}</div>
                          </div>

                          <div class="row">
                            <div class="col-md-6">
                              <div class="form-group-animated">
                                <label>Classe</label>
                                <div class="input-group">
                                  <div class="input-group-prepend">
                                    <span class="input-group-text"><i class="mdi mdi-book-open-page-variant"></i></span>
                                  </div>
                                  {{ form_widget(form.classe, {'attr': {
                                    'class': 'form-control',
                                    'value': user.classe
                                  }}) }}
                                </div>
                                {{ form_errors(form.classe) }}
                              </div>
                            </div>
                            <div class="col-md-6">
                              <div class="form-group-animated">
                                <label>Spécialité future</label>
                                <div class="input-group">
                                  <div class="input-group-prepend">
                                    <span class="input-group-text"><i class="mdi mdi-briefcase"></i></span>
                                  </div>
                                  {{ form_widget(form.specialiteFuture, {'attr': {
                                    'class': 'form-control',
                                    'value': user.specialiteFuture
                                  }}) }}
                                </div>
                                {{ form_errors(form.specialiteFuture) }}
                              </div>
                            </div>
                          </div>
                        </div>

                        <!-- Account Settings Section -->
                        <div class="form-section">
                          <h5 class="form-section-title">
                            <i class="mdi mdi-account-cog"></i> Paramètres du compte
                          </h5>
                          
                          <div class="current-value">
                            <small class="text-muted">Valeurs actuelles:</small>
                            <div><strong>Sexe:</strong> {{ user.sexe }} | <strong>Rôle:</strong> {{ user.role }}</div>
                          </div>

                          <div class="row">
                            <div class="col-md-6">
                              <div class="form-group-animated">
                                <label>Sexe</label>
                                <div class="input-group">
                                  <div class="input-group-prepend">
                                    <span class="input-group-text"><i class="mdi mdi-gender-male-female"></i></span>
                                  </div>
                                  {{ form_widget(form.sexe, {'attr': {
                                    'class': 'form-control'
                                  }}) }}
                                </div>
                                {{ form_errors(form.sexe) }}
                              </div>
                            </div>
                            <div class="col-md-6">
                              <div class="form-group-animated">
                                <label>Rôle</label>
                                <div class="input-group">
                                  <div class="input-group-prepend">
                                    <span class="input-group-text"><i class="mdi mdi-shield-account"></i></span>
                                  </div>
                                  {{ form_widget(form.role, {'attr': {
                                    'class': 'form-control'
                                  }}) }}
                                </div>
                                {{ form_errors(form.role) }}
                              </div>
                            </div>
                          </div>
                        </div>

                        <!-- Password Section (Optional) -->
                        <div class="form-section">
                          <h5 class="form-section-title">
                            <i class="mdi mdi-lock-reset"></i> Changer le mot de passe (Optionnel)
                          </h5>
                          
                          <div class="password-note">
                            <i class="mdi mdi-information"></i>
                            Laissez ce champ vide si vous ne souhaitez pas changer le mot de passe.
                          </div>

                          <div class="form-group-animated">
                            <label>Nouveau mot de passe</label>
                            <div class="input-group">
                              <div class="input-group-prepend">
                                <span class="input-group-text"><i class="mdi mdi-key"></i></span>
                              </div>
                              <input type="password" class="form-control" id="newPassword" 
                                     name="newPassword" placeholder="Laissez vide pour ne pas changer">
                              <div class="input-group-append">
                                <button class="btn btn-outline-secondary" type="button" id="togglePassword">
                                  <i class="mdi mdi-eye"></i>
                                </button>
                              </div>
                            </div>
                          </div>

                          <div class="form-group-animated">
                            <label>Confirmer le nouveau mot de passe</label>
                            <div class="input-group">
                              <div class="input-group-prepend">
                                <span class="input-group-text"><i class="mdi mdi-key-change"></i></span>
                              </div>
                              <input type="password" class="form-control" id="confirmNewPassword" 
                                     placeholder="Répétez le nouveau mot de passe">
                              <div class="input-group-append">
                                <button class="btn btn-outline-secondary" type="button" id="toggleConfirmPassword">
                                  <i class="mdi mdi-eye"></i>
                                </button>
                              </div>
                            </div>
                            <div class="error-message text-danger" id="passwordError" style="display: none;">
                              Les mots de passe ne correspondent pas
                            </div>
                            <div class="success-check text-success" id="passwordSuccess" style="display: none;">
                              <i class="mdi mdi-check-circle"></i> Les mots de passe correspondent
                            </div>
                          </div>
                        </div>

                        <!-- Action Buttons -->
                        <div class="form-section text-center">
                          <div class="d-flex justify-content-center gap-3">
                            <button type="submit" class="btn btn-update btn-icon-text">
                              <i class="mdi mdi-content-save btn-icon-prepend"></i>
                              Mettre à jour
                            </button>
                            <a href="{{ path('app_user_index') }}" class="btn btn-outline-secondary btn-icon-text">
                              <i class="mdi mdi-close btn-icon-prepend"></i>
                              Annuler
                            </a>
                          </div>
                        </div>

                      {{ form_end(form) }}
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
          <!-- content-wrapper ends -->
          <footer class="footer">
            <div class="d-sm-flex justify-content-center justify-content-sm-between">
              <span class="text-muted text-center text-sm-left d-block d-sm-inline-block">Copyright © 2024</span>
            </div>
          </footer>
        </div>
      </div>
    </div>
    <!-- plugins:js -->
    <script src="{{ asset('assets/vendors/js/vendor.bundle.base.js') }}"></script>
    <!-- Custom js -->
    <script src="{{ asset('assets/js/off-canvas.js') }}"></script>
    <script src="{{ asset('assets/js/misc.js') }}"></script>
    
    <script>
      // Password visibility toggle
      document.getElementById('togglePassword').addEventListener('click', function() {
        const passwordField = document.getElementById('newPassword');
        const icon = this.querySelector('i');
        if (passwordField.type === 'password') {
          passwordField.type = 'text';
          icon.classList.remove('mdi-eye');
          icon.classList.add('mdi-eye-off');
        } else {
          passwordField.type = 'password';
          icon.classList.remove('mdi-eye-off');
          icon.classList.add('mdi-eye');
        }
      });

      document.getElementById('toggleConfirmPassword').addEventListener('click', function() {
        const confirmField = document.getElementById('confirmNewPassword');
        const icon = this.querySelector('i');
        if (confirmField.type === 'password') {
          confirmField.type = 'text';
          icon.classList.remove('mdi-eye');
          icon.classList.add('mdi-eye-off');
        } else {
          confirmField.type = 'password';
          icon.classList.remove('mdi-eye-off');
          icon.classList.add('mdi-eye');
        }
      });

      // Password confirmation check
      document.getElementById('confirmNewPassword').addEventListener('input', checkPasswordMatch);
      document.getElementById('newPassword').addEventListener('input', checkPasswordMatch);

      function checkPasswordMatch() {
        const password = document.getElementById('newPassword').value;
        const confirmPassword = document.getElementById('confirmNewPassword').value;
        const errorElement = document.getElementById('passwordError');
        const successElement = document.getElementById('passwordSuccess');
        
        if (password === '' && confirmPassword === '') {
          errorElement.style.display = 'none';
          successElement.style.display = 'none';
          return;
        }
        
        if (password === confirmPassword) {
          errorElement.style.display = 'none';
          successElement.style.display = 'block';
        } else {
          errorElement.style.display = 'block';
          successElement.style.display = 'none';
        }
      }

      // Form validation
      document.getElementById('editForm').addEventListener('submit', function(e) {
        const password = document.getElementById('newPassword').value;
        const confirmPassword = document.getElementById('confirmNewPassword').value;
        
        if (password !== '' && password !== confirmPassword) {
          e.preventDefault();
          document.getElementById('passwordError').style.display = 'block';
          document.getElementById('passwordSuccess').style.display = 'none';
          document.getElementById('confirmNewPassword').focus();
        }
      });

      // Animation on focus
      const formInputs = document.querySelectorAll('.form-control');
      formInputs.forEach(input => {
        input.addEventListener('focus', function() {
          this.parentElement.parentElement.style.transform = 'scale(1.02)';
        });
        
        input.addEventListener('blur', function() {
          this.parentElement.parentElement.style.transform = 'scale(1)';
        });
      });
    </script>
  </body>
</html>