<form method="post" action="{{ path('app_suivi_bien_etre_delete', {'id': suivi_bien_etre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suivi_bien_etre.id) }}">
    <button class="btn">Delete</button>
</form>
