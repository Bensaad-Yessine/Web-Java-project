<div class="container">
    {{ form_start(form) }}
        <div class="row">
            <div class="col-md-8">
                {{ form_widget(form) }}
            </div>
        </div>
        <div class="mt-3">
            <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
        </div>
    {{ form_end(form) }}
</div>
